/// <reference types="vite/client" />

/** The version of `@42/ui-react` the console was built against — the one its previews
 *  render and the Parity tab measures. Baked by `vite.console.config.ts` from
 *  `vendor/ui-react/package.json`; `""` when it could not be read. */
declare const __KIT_VERSION__: string
