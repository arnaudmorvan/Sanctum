/**
 * Shared horizontal alignment vocabulary — `Divider`'s label position and
 * `DataTable`'s column alignment both mean the same thing (start / centered /
 * end), so they share one type instead of each declaring an identical union.
 */
export type Align = "start" | "center" | "end";
//# sourceMappingURL=align.d.ts.map