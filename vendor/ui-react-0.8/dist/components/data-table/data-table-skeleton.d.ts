/**
 * Shared skeleton visual language — shimmer, fill, corner radius. Both the
 * scroll-table and stacked-card default bars now render through the shared
 * `Skeleton` component (`../skeleton`), which owns sizing itself. This
 * constant is kept purely so a custom `meta.skeleton` shape can hand-match
 * its exact tokens for whatever real, non-text footprint it stands in for
 * (a thumbnail, an action button, …) instead of hand-copying them.
 */
export declare const DATA_TABLE_SKELETON_CLASS = "animate-pulse rounded-xs bg-black/15 dark:bg-white/8";
//# sourceMappingURL=data-table-skeleton.d.ts.map