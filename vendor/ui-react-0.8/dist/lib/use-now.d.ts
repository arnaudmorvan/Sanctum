/**
 * Live-ticking current time, or a frozen `override` when one is passed —
 * with genuinely no timers set up in that case, since a fixed time for
 * tests/Storybook shouldn't visually drift or tick.
 */
export declare function useNow(override?: Date): Date;
//# sourceMappingURL=use-now.d.ts.map